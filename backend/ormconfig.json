{
    "type": "postgres",
    "host": "localhost",
    "port": "5432",
    "username": "admin",
    "password": "123456",
    "database": "postgres",
    "logging": false,
    "migrations": [
        "src/database/migrations/*.{js,ts}"
    ],
    "entities": [
        "src/models/*.{js,ts}"
    ],
    "cli": {
        "migrationsDir": "src/database/migrations",
        "entitiesDir": "src/models"
    }
}